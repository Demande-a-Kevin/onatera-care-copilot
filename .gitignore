# OS
.DS_Store
Thumbs.db

# Editeurs
.vscode/
.idea/
*.swp

# Logs runtime
/tmp/ollama-care.log
*.log

# Worker (outillage wrangler)
live-bridge/.wrangler/
node_modules/

# Secrets : jamais dans le depot.
# - DEMO_TOKEN / ORIGIN_SECRET du pont live = secrets Cloudflare (wrangler secret put ...)
# - live-bridge/.secret contient ORIGIN_SECRET en local (partage avec le proxy d'origine).
live-bridge/.secret
*.secret
